{
  "name": "alp-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-agnostic shadcn/ui component library built on Alpine.js and Tailwind CSS",
  "scripts": {
    "build": "npx tailwindcss -i ./src/input.css -o ./dist/ui.css --minify && cp -r src/themes/. dist/themes/ && cp src/alp-ui.js dist/alp-ui.js",
    "dev": "npx tailwindcss -i ./src/input.css -o ./dist/ui.css --watch",
    "prebuild": "mkdir -p dist"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "tailwindcss": "^4.1.4"
  }
}
